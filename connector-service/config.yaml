backend:
  url: ${BACKEND_WS_URL}
  api_key: ${CONNECTOR_API_KEY}
  reconnect_interval: 5
  heartbeat_interval: 30
  insecure_skip_verify: false

ews:
  url: ${EWS_URL}
  domain: ${EWS_DOMAIN}
  username: ${EWS_USERNAME}
  password: ${EWS_PASSWORD}
  skip_verify: ${EWS_SKIP_TLS_VERIFY}
